# Optional. If unset, the app uses Metro’s LAN host (works for most physical devices).
# EXPO_PUBLIC_API_URL=http://192.168.1.10:3000

# Optional. Same as Vula24 — Android Maps key for com.vula24.pro (app.config.js).
# GOOGLE_MAPS_ANDROID_KEY=
